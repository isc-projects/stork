import { ManagedAccessDirective } from './managed-access.directive'

describe('ManagedAccessDirective', () => {
    it('should create an instance', () => {
        // const directive = new ManagedAccessDirective()
        // expect(directive).toBeTruthy()
    })
})
