/**
 * An interface to a representation of a client class that can be
 * filtered and selected via a multi-select list or a table.
 */
export interface SelectableClientClass {
    /**
     * Class name.
     */
    name: string
}
