/**
 * An interface to a representation of a shared network that can be
 * selected from a drop down list.
 */
export interface SelectableSharedNetwork {
    /**
     * Shared network ID.
     */
    id: number

    /**
     * Shared network name.
     */
    name: string
}
