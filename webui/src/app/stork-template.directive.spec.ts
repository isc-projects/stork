import { StorkTemplateDirective } from './stork-template.directive'

describe('StorkTemplateDirective', () => {
    it('should create an instance', () => {
        const directive = new StorkTemplateDirective()
        expect(directive).toBeTruthy()
    })
})
