#!/bin/sh

if [ -x "$(command -v systemctl)" ] && [ "$(systemctl is-system-running)" -eq 0 ]; then
    systemctl disable isc-stork-agent
    systemctl stop isc-stork-agent
fi

usermod -G "" stork-agent
