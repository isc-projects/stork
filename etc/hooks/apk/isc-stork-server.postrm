#!/bin/sh

set -eu

# Some installs may self-manage the stork-server user, so only remove if the GECOS matches
if [ "$(getent passwd stork-server | cut -d : -f 5)" = "Stork Server User" ]; then
    deluser stork-server >/dev/null || /bin/true
    delgroup stork-server >/dev/null || /bin/true
fi
